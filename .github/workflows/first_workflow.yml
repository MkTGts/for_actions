name: First workflowon
on: workflow_dispatch

jobs:
  run-first-command:
    runs-on: ubuntu-latest
    steps:
      - name: Display some Text
        run: echo "Some text."
      - name: Command in Multiline
        run: |-
          echo Hello World!
          node -v
          npm -v 
          python3 --version

